# gobox Command Design

本文档定义 gobox 命令体系的设计规范，回答“为什么收这些命令、为什么只收这些参数、哪些能力明确不做”。

`docs/CMD-SPECS.md` 是命令级用户可见语义的唯一规格源：支持矩阵、兼容性标签、差异边界和 gobox 扩展语义都只在 `docs/CMD-SPECS.md` 中定义；本文只负责定义命令设计原则与范围边界。

---

## 设计原则

gobox 的目标不是完整替代 BusyBox 或 coreutils，而是在容器、精简镜像、临时排障环境中补齐常见缺失命令，提供足够实用的诊断和处置能力。

命令选择遵循三个约束：

1. 精简镜像通常缺失：优先补充 slim/distroless/debug 场景里经常不可用，但排障时很需要的工具。
2. 容器排障价值高：优先覆盖网络、进程、文件、文本、磁盘 I/O 等常见定位路径。
3. 最常用 + 不膨胀 + 精简：只实现高频参数和核心语义，避免引入完整系统管理套件或低频高级参数。

排除范围同样明确：

1. 不纳入系统管理命令：不实现用户管理、init/service、内核模块、文件系统修复、设备管理、网络配置变更等宿主机/系统级管理能力。
2. 不重复镜像里通常已有的基础命令：如 `cat`、`ls`、`cp`、`mv`、`rm`、`mkdir`、`echo`、`printf`、`test`、`true`、`false`、`sleep` 等基础 shell/coreutils 能力，除非后续证明它们在目标环境中经常缺失且排障价值显著。

因此，gobox 的设计重点是“够用且可验证”的常用能力，而不是追求对所有原生命令参数的完整复刻。

---

## 文档分工

- `docs/CMD-DESIGN.md`：定义命令设计原则、收敛边界和不做事项
- `docs/CMD-SPECS.md`：定义支持矩阵、兼容性标签、差异边界和用户可见语义
- `docs/TEST-DESIGN.md`：定义如何用自动化测试证明 `docs/CMD-SPECS.md`
- `docs/TEST-CASES.md`：定义规格到具体测试案例的映射
